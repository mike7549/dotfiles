win_reboot () {
	sudo efibootmgr -n 0000
	reboot
}

update_all () {
	yay -Syu --answerdiff N --answerclean N --answeredit N --devel
	omz update
	sudo nvim -u ~/dotfiles/vim/init.vim -c :PlugUpgrade -c :PlugUpdate -c :q -c :q
}
